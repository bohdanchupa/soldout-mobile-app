# 🔧 Вирішення помилок збірки iOS

## Найчастіші помилки та рішення

### ❌ Помилка 1: "Signing requires a development team"

**Причина**: Не вибрано Team (Apple ID) для підпису додатку

**Рішення**:
1. У Xcode: Виберіть проект **"App"** у лівій панелі (синя іконка)
2. Вкладка **"Signing & Capabilities"**
3. У розділі **"Signing"**:
   - Поставте галочку **"Automatically manage signing"**
   - У полі **"Team"** виберіть ваш Apple ID
   - Якщо вашого Apple ID немає в списку:
     - Натисніть **"Add Account..."**
     - Увійдіть з вашим Apple ID
     - Поверніться до Signing & Capabilities
     - Виберіть ваш Apple ID

4. Якщо з'явиться помилка про Bundle Identifier:
   - Змініть **Bundle Identifier** на унікальний (наприклад: `com.yourname.soldoutstatistics.app`)

---

### ❌ Помилка 2: "No signing certificate found"

**Причина**: Немає сертифіката для підпису

**Рішення**:
1. У Xcode: **Xcode → Settings (Preferences) → Accounts**
2. Виберіть ваш Apple ID
3. Натисніть **"Download Manual Profiles"**
4. Перевірте чи є сертифікат у списку
5. Якщо немає - Xcode автоматично створить при першій збірці

---

### ❌ Помилка 3: "Could not find iPhone" або пристрій не видно

**Причина**: iPhone не підключено або не довіряє Mac

**Рішення**:
1. Перевірте USB кабель (використовуйте оригінальний)
2. Розблокуйте iPhone
3. На iPhone: Натисніть **"Trust This Computer"** коли з'явиться запит
4. У Xcode: **Window → Devices and Simulators**
5. Перевірте чи iPhone з'являється у списку
6. Якщо ні - перезапустіть Xcode та iPhone

---

### ❌ Помилка 4: "Provisioning profile doesn't match"

**Причина**: Профіль підпису не відповідає Bundle ID

**Рішення**:
1. У Xcode: **Signing & Capabilities**
2. Зніміть галочку **"Automatically manage signing"**
3. Поставте галочку знову
4. Виберіть ваш Team
5. Xcode автоматично створить новий профіль

---

### ❌ Помилка 5: "Build failed" з незрозумілими помилками

**Причина**: Різні проблеми (кеш, залежності, конфігурація)

**Рішення (покроково)**:

#### Крок 1: Clean Build
```bash
# У Xcode:
Product → Clean Build Folder (⌘⇧K)
```

#### Крок 2: Перевірте файли
```bash
# Переконайтеся що файли оновлені
cd "/Users/bohdanchupa/Desktop/SoldOut/Додаток орга/org-mobile-app"
docker exec soldout-app-container sh -c "npx quasar build" > /dev/null 2>&1
cp -r dist/spa/* src-cordova/www/
cp -r src-cordova/www/* src-cordova/platforms/ios/www/
```

#### Крок 3: Перезапустіть Xcode
- Закрийте Xcode повністю
- Відкрийте знову: `open src-cordova/platforms/ios/App.xcworkspace`

#### Крок 4: Перевірте Signing
- Signing & Capabilities → Team: [Ваш Apple ID]
- Bundle Identifier: `com.soldoutstatistics.app` (або змініть на унікальний)

#### Крок 5: Спробуйте зібрати знову
- Product → Clean Build Folder (⌘⇧K)
- Product → Run (⌘R)

---

## 📋 Чек-лист перед збіркою

- [ ] iPhone підключено через USB
- [ ] iPhone розблоковано і довіряє Mac
- [ ] У Xcode вибрано iPhone як пристрій (не "Any iOS Device")
- [ ] Signing & Capabilities → Team вибрано (ваш Apple ID)
- [ ] Bundle Identifier унікальний
- [ ] Файли оновлені (web assets скопійовані в `platforms/ios/www/`)
- [ ] Clean Build Folder виконано (⌘⇧K)

---

## 🆘 Якщо нічого не допомагає

1. **Видаліть Derived Data**:
   ```bash
   rm -rf ~/Library/Developer/Xcode/DerivedData/*
   ```

2. **Перебудуйте платформу iOS**:
   ```bash
   cd src-cordova
   rm -rf platforms/ios
   # Потрібно буде додати платформу знову (якщо є Node.js)
   ```

3. **Перевірте версію Xcode**:
   - Xcode → About Xcode
   - Має бути версія 14.0 або новіша

4. **Перевірте версію iOS на iPhone**:
   - Має бути iOS 13.0 або новіша (згідно з `deployment-target`)

---

## 💡 Корисні команди

```bash
# Оновити файли перед збіркою
cd "/Users/bohdanchupa/Desktop/SoldOut/Додаток орга/org-mobile-app"
docker exec soldout-app-container sh -c "npx quasar build" > /dev/null 2>&1
cp -r dist/spa/* src-cordova/www/
cp -r src-cordova/www/* src-cordova/platforms/ios/www/

# Відкрити Xcode
open src-cordova/platforms/ios/App.xcworkspace

# Перевірити підключені пристрої
system_profiler SPUSBDataType | grep -i iphone
```

---

## 📞 Якщо все ще не працює

Надішліть мені:
1. Точний текст помилки з Xcode (кнопка помилки → Copy)
2. Скріншот Signing & Capabilities
3. Версію Xcode та iOS на iPhone
