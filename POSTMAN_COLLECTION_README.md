# Soldout Statistics API - Postman Collection

Повна колекція API ендпоінтів для Soldout Statistics Mobile App з автоматичною автентифікацією.

## 📋 Зміст

1. [Швидкий старт](#швидкий-старт)
2. [Структура колекції](#структура-колекції)
3. [Детальний аналіз API](#детальний-аналіз-api)
4. [Автентифікація](#автентифікація)
5. [Ендпоінти](#ендпоінти)
6. [Приклади використання](#приклади-використання)

---

## 🚀 Швидкий старт

### 1. Імпорт колекції та environment

1. Відкрийте Postman
2. Натисніть **Import** → виберіть файли:
   - `Soldout_API_Collection.postman_collection.json`
   - `Soldout_API_Environment.postman_environment.json`
3. Виберіть environment **"Soldout API - Test Environment"** у правому верхньому куті

### 2. Налаштування environment змінних

Всі змінні вже налаштовані, але ви можете їх змінити:

| Змінна | Значення за замовчуванням | Опис |
|--------|---------------------------|------|
| `base_url` | `https://z.test.soldout.com.ua` | Базовий URL API |
| `username` | `d.crosspost@gmail.com` | Email для автентифікації |
| `password` | `aXLYjbfTNNN_soldout584` | Пароль |
| `eventId` | (порожнє) | ID події для тестування |
| `dateFrom` | (порожнє) | Початкова дата |
| `dateTo` | (порожнє) | Кінцева дата |

### 3. Перший запит

1. Перейдіть до **Authentication → Get OAuth Token**
2. Натисніть **Send**
3. Токен автоматично збережеться в environment змінну `access_token`
4. Тепер всі інші запити будуть автоматично використовувати цей токен

---

## 📁 Структура колекції

```
Soldout Statistics API
├── Authentication
│   ├── Get OAuth Token          # Отримання токену
│   └── Get User Info            # Інформація про користувача
│
├── Dashboard & Statistics
│   ├── Get Summary by Period    # Загальна статистика
│   ├── Get All Orders Count     # Кількість замовлень
│   ├── Get All Tickets Count    # Кількість квитків
│   └── Get Statistics for Entry Screen  # Статистика для графіків
│
├── Events
│   ├── Get Bought Events        # Куплені події
│   ├── Get Events Today         # Події за сьогодні
│   └── Get Archive Events       # Архівні події
│
└── Event Details
    ├── Get Event Sum (Money)    # Статистика по сумі
    ├── Get Event Count (Tickets) # Статистика по кількості
    ├── Get Event Details Table (All Ticket Types)  # Всі типи квитків
    ├── Get Event Details Table For Date            # Деталі за дату
    └── Get Event Sold Short Info                   # Коротка інформація про продажі
```

---

## 🔍 Детальний аналіз API

### Автентифікація

**Тип:** OAuth 2.0 Password Grant Flow

**Процес:**
1. Клієнт надсилає запит на `/main-service/oauth/token` з credentials
2. Сервер повертає `access_token` та `expires_in`
3. Токен використовується в заголовку `Authorization: Bearer {token}`

**Важливі деталі:**
- **Basic Auth:** `Basic  YWRtaW4tY2xpZW50OjEyMzQ1Ng==` (зверніть увагу на пробіл після "Basic")
- **Custom Header:** `X-SOLDOUT-STATISTIC-APP-ACCESS: b9011a8e5cdb1b62ec5513e2303c2b78`
- Токен автоматично оновлюється через Pre-request Script перед кожним запитом

### Автоматичне оновлення токену

Колекція містить **Pre-request Script**, який:
- Перевіряє наявність та валідність токену
- Автоматично отримує новий токен, якщо він відсутній або прострочений
- Зберігає токен в environment змінну

**Логіка:**
```javascript
if (!token || token_expired) {
    // Отримати новий токен
    // Зберегти в environment
}
```

---

## 📡 Ендпоінти

### Authentication

#### 1. Get OAuth Token
```
POST /main-service/oauth/token
```

**Параметри:**
- `username` - email користувача
- `password` - пароль
- `grant_type` - завжди "password"

**Відповідь:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer",
  "expires_in": 3600,
  "scope": "..."
}
```

#### 2. Get User Info
```
GET /main-service/user/get
```

**Відповідь:**
```json
{
  "firstName": "Ім'я",
  "lastName": "Прізвище",
  "mail": "email@example.com",
  "phone": "+380123456789"
}
```

---

### Dashboard & Statistics

#### 3. Get Summary by Period
```
GET /main-service/api/find-sum-by-organizator
```

**Query параметри:**
- `dateFrom` - початкова дата (ISO формат: `YYYY-MM-DDTHH:mm:ss`)
- `dateTo` - кінцева дата

**Відповідь:**
```json
{
  "allTicketSum": 150000,
  "notSoldTicketSum": 50000,
  "offlineTicketSum": 80000,
  "onlineTicketSum": 70000,
  "inviteTicketSum": 10000,
  "bookedTicketSum": 20000,
  "hardLockedTicketSum": 15000
}
```

**Використання:** Головний екран (Entry.vue) - відображення загальної статистики

---

#### 4. Get All Orders Count
```
GET /main-service/api/find-orders-by-organizator
```

**Query параметри:**
- `dateFrom`, `dateTo`

**Відповідь:**
```json
{
  "allOrderCount": 150
}
```

---

#### 5. Get All Tickets Count
```
GET /main-service/api/find-tickets-by-organizator-short
```

**Query параметри:**
- `dateFrom`, `dateTo`

**Відповідь:**
```json
{
  "allTicketCount": 500
}
```

---

#### 6. Get Statistics for Entry Screen
```
GET /main-service/api/find-events-by-organizator-main-short-info
```

**Query параметри:**
- `dateFrom` - дата від якої отримувати статистику (зазвичай 7 днів назад)

**Відповідь:**
```json
[
  {
    "statisticDayTimestamp": "2024-01-08",
    "soldTickets": 45,
    "bookedTickets": 12,
    "soldTicketMoney": 15000
  },
  ...
]
```

**Використання:** Побудова графіків на головному екрані (7 днів)

---

### Events

#### 7. Get Bought Events
```
GET /main-service/api/find-bought-events-by-organizator
```

**Query параметри:**
- `dateFrom`, `dateTo`

**Відповідь:**
```json
[
  {
    "id": 123,
    "eventName": { "valueUA": "Назва події" },
    "cityName": { "valueUA": "Київ" },
    "hallName": { "valueUA": "Концертний зал" },
    "beginDate": "2024-01-15T19:00:00",
    "organizator": {
      "firstName": "Ім'я",
      "lastName": "Прізвище"
    },
    "ordersCount": 50,
    "ticketsCount": 200,
    "moneysSum": 50000,
    "mainImageSmall": {
      "path": "/path/to/image.jpg"
    }
  },
  ...
]
```

**Використання:** Список подій (ListOfEvents.vue) - загальні події

---

#### 8. Get Events Today
```
GET /main-service/api/find-bought-events-and-info-by-organizator
```

**Відповідь:** Аналогічна до Get Bought Events, але тільки за сьогодні

**Використання:** Список подій за сьогодні

---

#### 9. Get Archive Events
```
GET /main-service/api/find-archive-events-by-organizator
```

**Query параметри:**
- `dateFrom`, `dateTo`

**Відповідь:** Аналогічна до Get Bought Events

**Використання:** Архів подій

---

### Event Details

#### 10. Get Event Sum (Money)
```
GET /main-service/api/find-sum-by-event/{eventId}
```

**Query параметри (опціонально):**
- `dateFrom`, `dateTo` - для фільтрації за датою

**Відповідь:**
```json
{
  "allTicketSum": 100000,
  "notSoldTicketSum": 30000,
  "offlineTicketSum": 60000,
  "onlineTicketSum": 40000,
  "inviteTicketSum": 5000,
  "bookedTicketSum": 10000,
  "hardLockedTicketSum": 8000
}
```

**Використання:** EventDetails.vue - відображення статистики по сумі (графік та легенда)

---

#### 11. Get Event Count (Tickets)
```
GET /main-service/api/find-count-by-event/{eventId}
```

**Query параметри (опціонально):**
- `dateFrom`, `dateTo`

**Відповідь:**
```json
{
  "allTicketCount": 500,
  "notSoldTicketCount": 150,
  "allBoughtTicketCount": 350,
  "offlineTicketCount": 200,
  "onlineTicketCount": 150,
  "inviteTicketCount": 25,
  "bookedTicketCount": 50,
  "hardLockedTicketCount": 40
}
```

**Використання:** EventDetails.vue - відображення статистики по кількості (графік та легенда)

---

#### 12. Get Event Details Table (All Ticket Types)
```
GET /main-service/api/find-color-price-by-event/{eventId}
```

**Відповідь:**
```json
[
  {
    "color": "#FF608E",
    "price": 500,
    "availableTickets": 100
  },
  {
    "color": "#66E8CD",
    "price": 300,
    "availableTickets": 150
  },
  ...
]
```

**Важливо:** Цей ендпоінт використовується як **базова структура** для таблиці "Додаткова інформація". Він визначає всі типи квитків події.

**Використання:** EventDetails.vue - таблиця "Додаткова інформація" (рядки: Ціна квитка, Квота, Квота на суму)

---

#### 13. Get Event Details Table For Date
```
GET /main-service/api/find-color-price-by-event-and-date/{eventId}
```

**Query параметри:**
- `dateFrom`, `dateTo` - обов'язкові

**Відповідь:**
```json
[
  {
    "color": "#FF608E",
    "price": 500,
    "inviteTickets": 5,
    "bookedTickets": 10,
    "soldTickets": 50
  },
  {
    "color": "#66E8CD",
    "price": 300,
    "inviteTickets": 3,
    "bookedTickets": 8,
    "soldTickets": 40
  },
  ...
]
```

**Важливо:** Цей ендпоінт повертає дані за конкретну дату. У виправленому коді дані з цього ендпоінту зіставляються з базовою структурою (ендпоінт #12) за `color` та `price`.

**Використання:** EventDetails.vue - таблиця "Додаткова інформація" (рядки: Запрошення, Продано, Заброньовано)

---

#### 14. Get Event Sold Short Info
```
GET /main-service/api/find-events-by-organizator-sold-short-info/{eventId}
```

**Відповідь:**
```json
[
  {
    "soldTicketsMoney": 15000
  },
  {
    "soldTicketsMoney": 20000
  },
  ...
]
```

**Використання:** EventDetails.vue - побудова графіка продажів по днях (для загальних подій)

---

## 🔧 Приклади використання

### Приклад 1: Отримання статистики за сьогодні

1. Встановіть змінні в environment:
   ```
   dateFrom = 2024-01-15T00:00:00
   dateTo = 2024-01-15T23:59:59
   ```

2. Виконайте запити:
   - Get Summary by Period
   - Get All Orders Count
   - Get All Tickets Count

### Приклад 2: Отримання деталей події

1. Спочатку отримайте список подій:
   - Get Events Today або Get Bought Events
   - Скопіюйте `id` з відповіді

2. Встановіть змінну:
   ```
   eventId = 123
   ```

3. Виконайте запити Event Details:
   - Get Event Sum (Money)
   - Get Event Count (Tickets)
   - Get Event Details Table (All Ticket Types)
   - Get Event Details Table For Date (з dateFrom/dateTo)

### Приклад 3: Тестування виправлення таблиці

1. Встановіть `eventId` та `dateFrom`/`dateTo`
2. Виконайте:
   - Get Event Details Table (All Ticket Types) - базова структура
   - Get Event Details Table For Date - дані за дату
3. Перевірте, що:
   - Обидва ендпоінти повертають однакові `color` та `price`
   - Кількість елементів співпадає
   - Дані правильно зіставляються

---

## 🐛 Відомі проблеми та рішення

### Проблема: Токен не оновлюється автоматично

**Рішення:**
1. Перевірте, чи вибрано правильний environment
2. Переконайтеся, що Pre-request Script увімкнено на рівні колекції
3. Виконайте запит "Get OAuth Token" вручну

### Проблема: 401 Unauthorized

**Рішення:**
1. Перевірте правильність credentials в environment
2. Оновіть токен через "Get OAuth Token"
3. Перевірте заголовок `X-SOLDOUT-STATISTIC-APP-ACCESS`

### Проблема: Дані в таблиці не відповідають

**Рішення:**
Це була проблема, яку ми виправили! Переконайтеся, що:
- Використовується правильний порядок викликів API
- Дані зіставляються за `color` та `price`
- Базова структура береться з `find-color-price-by-event`

---

## 📝 Нотатки

1. **Формат дат:** Всі дати у форматі ISO: `YYYY-MM-DDTHH:mm:ss`
   - Приклад: `2024-01-15T00:00:00`

2. **Автентифікація:** Всі запити (крім Get OAuth Token) вимагають Bearer token

3. **Custom Header:** Всі запити вимагають заголовок `X-SOLDOUT-STATISTIC-APP-ACCESS`

4. **Environment:** Для продакшн змініть `base_url` на `https://z.soldout.com.ua`

5. **Токен:** Автоматично оновлюється перед кожним запитом, якщо прострочений

---

## 🔐 Безпека

⚠️ **Важливо:**
- Не комітьте файли з реальними credentials в Git
- Використовуйте environment змінні для чутливих даних
- Токени автоматично оновлюються, але перевіряйте їх валідність
- Custom header `X-SOLDOUT-STATISTIC-APP-ACCESS` є обов'язковим

---

## 📚 Додаткові ресурси

- **Base URL (Test):** `https://z.test.soldout.com.ua`
- **Base URL (Prod):** `https://z.soldout.com.ua`
- **OAuth Endpoint:** `/main-service/oauth/token`
- **API Prefix:** `/main-service/api/`

---

## ✅ Чеклист для тестування

- [ ] Імпортовано колекцію та environment
- [ ] Отримано токен через "Get OAuth Token"
- [ ] Перевірено "Get User Info"
- [ ] Протестовано Dashboard ендпоінти
- [ ] Протестовано Events ендпоінти
- [ ] Протестовано Event Details ендпоінти
- [ ] Перевірено виправлення таблиці (ендпоінти #12 та #13)

---

**Створено:** 2024  
**Версія:** 1.0  
**Автор:** AI Assistant
