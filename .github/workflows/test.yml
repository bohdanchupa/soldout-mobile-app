name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: List files
        run: |
          echo "Files in repo:"
          ls -la
          echo "Package.json exists:"
          cat package.json | head -20

